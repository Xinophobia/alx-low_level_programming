#include <stdio.h>
/**
 * main - prints all the alphabets in lowercase
 * Return: 0
 */

char main(void)
{
	char alpha[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (i = a; i < 26; i++)
	{
		putchar(alpha[i]);
	}
	putchar('\n')
	return (0);
}
